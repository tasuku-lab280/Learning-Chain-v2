<% if post.liked_by?(current_user) %>
  <p>
		<%= link_to "💓", post_like_path(post.id), method: :delete, remote: true %>
		<strong><%= post.likes.count %></strong>
	</p>
<% else %>
  <p>
		<%= link_to "💓", post_likes_path(post.id), method: :post, remote: true %>
		<strong><%= post.likes.count %></strong>
	</p>
<% end %>
