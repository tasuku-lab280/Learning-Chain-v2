<div class="col-md-8">

  <div class="search-form">
    <%= form_with url: root_path, method: :get, local: true do |f| %>
      <%= f.label :search, '投稿タイトル検索' %>
      <div>
        <%= f.text_field :search %>
        <%= f.submit '検索' %>
      </div>
    <% end %>
  </div>

  <% if logged_in? %>
    <%= render 'shared/feed' %>
  <% else %>
    <% @posts.each do |post| %>
      <ol class="posts">
        <%= render 'homes/post', post: post %>
      </ol>
    <% end %>
    <%= will_paginate @posts %>
  <% end %>

</div>


<div class="col-md-4 right-box">

  <% if logged_in? %>
    <% @user ||= current_user %>

    <%= render 'shared/stats' %>

    <% if @user.likes.any? %>
      <div class="likes">
        <h3>いいねした投稿</h3>
        <% @user.liked_posts.each do |post| %>
          <ul>
            <%= link_to post_path(post.id) do %>
              <%= post.user.name %>:<%= post.title %>
            <% end %>
          </ul>
        <% end %>
      </div>
    <% end %>

  <% else %>
    <h1>Learning Chain</h1>
    <p>学習記録SNSアプリケーションです。</p>
    <p>タイマーで勉強時間を測って投稿してみましょう！</p>
    <p>ログインすると投稿できます！</p>
    
    <div class="button-box">
      <div>
        <p>まだユーザー登録がお済でない方↓</p>
        <div>
          <%= link_to signup_path, class:"btn-flat-logo-signup" do %>
            新規登録ページへ
          <% end %>
        </div>
      </div>

      <div class="login-button">
        <p>ユーザー登録がお済の方↓</p>
        <div>
          <%= link_to login_path, class:"btn-flat-logo-login" do %>
            ログインページへ
          <% end %>
        </div>
      </div>
    </div>
  <% end %>

</div>