defaults: &defaults
  fog_provider: 'AWS'
  aws_access_key_id: 'AKIAQWA2M3HQTYGJ5CMY'
  aws_secret_access_key: '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'
  # To use AWS reduced redundancy storage.
  # aws_reduced_redundancy: true
  fog_directory: "develop-learning-chain-app" # バケット名を指定
  # You may need to specify what region your storage bucket is in
  fog_region: "ap-northeast-1"
  existing_remote_files: keep
  # To delete existing remote files.
  # existing_remote_files: delete
  # Automatically replace files with their equivalent gzip compressed version
  # gzip_compression: true
  # Fail silently.  Useful for environments such as Heroku
  # fail_silently: true

development:
  <<: *defaults
  enabled: false

test:
  <<: *defaults
  enabled: false

staging:
  <<: *defaults
  fog_directory: "staging-learning-chain-app"

production:
  <<: *defaults
  fog_directory: "production-learning-chain-app"
